● -What is `NPM`?

npm is package manager where it have lot of reprositries,it help devlopers manage dependies 

● - What is `Parcel/Webpack`? Why do we need it?

both are module bluders , 
They combine multiple JavaScript files (modules) into a single file or a few files, ensuring the browser can load them efficiently.
Handle and optimize non-JavaScript assets like images, fonts, and CSS, making it easier to include them in your application.
Hot Module Replacement (HMR): Automatically updates the browser without a full reload when you make changes.
Live Reloading: Speeds up development by refreshing the browser whenever files change.

● - What is `.parcel-cache`

It stores intermediate build artifacts and metadata to avoid redundant work during subsequent builds.
it holds the cache ,  It stores intermediate build artifacts and metadata to avoid redundant work during subsequent builds.

● - What is `npx` ?

npx is executing our packages 

● - What is difference between `dependencies` vs `devDependencies`

dependencies uses in production places 
dev dependencies uses in devlopment phase

● - What is Tree Shaking?

removes the unwanted functions 

● - What is Hot Module Replacement?

we dont need to reload the page its automatically reload the page 
Hot Module Replacement (HMR): Automatically updates the browser without a full reload when you make changes.


● - List down your favourite 5 superpowers of Parcel and describe any 3 of them in your
own words.

● - What is `.gitignore`? What should we add and not add into it?

gnores the files to push in git 

● - What is the difference between `package.json` and `package-lock.json`

package.json create when we intialize the npm init and package -lock.json create when we install any packages,

package json controls the versions and it have ebility to increase the version and 

package.lock .json contains the total version details in detail and it collects the all installs and keep the total details init

● - Why should I not modify `package-lock.json`?

The package-lock.json file is automatically generated by npm whenever you install or modify dependencies in your project.
It records the exact versions of dependencies and their transitive (nested) dependencies, along with metadata such as resolved URLs and integrity hashes.

● - What is `node_modules` ? Is it a good idea to push that on git?

node modules contains the data, what ever we are installing in our code and it doesnt have to push in our github we can easily install again npm install by using package.json and packae.lock.json.

It contains the actual code and files for the packages listed in the dependencies and devDependencies sections of your package.json file. 

● - What is the `dist` folder?

when we run the our code by using the npx parcel index.html its create the dist folder,

It contains the optimized, compiled, and minified files that are ready to be deployed to a web server or delivered to users. 

● - What is `browserlists`

uses to which version of brwosers to use.

Read about dif bundlers: vite, webpack, parcel these are module bundles 

● Read about: ^ - caret and ~ - tilda ^ for small upgrades ~ for big updates 

● Read about Script types in html (MDN Docs)